<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Thème principal sans barre d'action -->
    <style name="Theme.Beztamy.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Couleurs principales -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Couleurs secondaires -->
        <item name="colorSecondary">@color/accent_color</item>
        <item name="colorSecondaryVariant">@color/accent_dark</item>
        <item name="colorOnSecondary">@android:color/white</item>

        <!-- Couleur de la barre de statut -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/primary_dark</item>
    </style>

    <!-- Overlay pour AppBar -->
    <style name="MyStyle.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Overlay pour les popups -->
    <style name="MyStyle.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
